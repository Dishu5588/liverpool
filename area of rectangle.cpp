#include <stdio.h>
int main()
{
	float a,l,b;
	printf("/n enter the length:");
	scanf("%f",&l);
	printf("enter the breath:");
	scanf("%f",&b);
	a=l*b;
	printf("area of rect is %f",a);
	return 0;
}
